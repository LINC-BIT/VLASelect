在api文件夹以及对应的运行程序（api/unified_online_rl.py）中，将小模型的生成（即should_regenerate_small_model_before_rollout判断及判断True后的运行逻辑）抽取出来作为一个单独的类`SmallModelScalingInterface`。该类的默认实现即api/unified_online_rl.py中的目前实现。该类放在api/small_model_scaling_interface.py中。该类的默认实现应该能够支持api/unified_online_rl.py、api/vla_model_interface_examples/tinyvla_impl_verify.sh、api/vla_model_interface_examples/vla_adapter_impl_verify.sh等脚本的正常运行。

该类只是允许用户能够自定义小模型的生成流程。你补充一个自定义实现的例子，在api/small_model_scaling_interface_examples中新增一个`knowledge_distillation_impl.py`，即通过知识蒸馏来生成小模型的例子。具体的，该方法完全复用api/unified_online_rl.py中的目前实现，只是在小模型生成之后，再补充一定轮次的知识蒸馏训练过程。知识蒸馏涉及的所有超参数都不要通过方法参数传递，仅在方法类初始化的时候指定。也提供对应的试运行脚本`knowledge_distillation_impl_verify.sh`。该脚本也支持MWE模式（模仿api/vla_model_interface_examples/vla_adapter_impl_verify.sh，不改变运行逻辑，只改变时间限制）。

除此之外，还可以有其它的辅助文件。原则就是，你不能改动除了api文件夹下的其它任何文件！如果需要改动，则将其复制到api文件夹中改动并引用。坚决不能改动api文件夹之外的任何文件！请保证api文件夹下的脚本和程序能够独立运行，不依赖其它文件夹。